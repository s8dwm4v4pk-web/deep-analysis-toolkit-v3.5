{
  "name": "list_dir",
  "type": "platform",
  "category": "io",
  "description": "列出指定目录下的文件和子目录",
  "parameters": {
    "target_directory": { "type": "string", "required": true, "description": "要列出内容的目录路径" },
    "ignore_globs": { "type": "array", "required": false, "description": "要忽略的 glob 模式列表" }
  },
  "returns": { "type": "array", "description": "目录内容列表（隐藏文件默认不显示）" },
  "stageUsage": ["all"]
}
